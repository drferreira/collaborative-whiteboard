angular.module('whiteboard-module').controller('WhiteboardStagesCtrl', ['$scope', '$http', '$window', '$rootScope', function ($scope, $http, $window, $rootScope) {
    console.log($scope.cwStages);
}]);
